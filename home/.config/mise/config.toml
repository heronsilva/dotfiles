[tools]
lua = "latest"
node.version = "25"
node.postinstall = "command -v corepack >/dev/null || npm install -g corepack && corepack enable pnpm"
python = "3.14"
usage = "latest"

[hooks]
enter = "mise install --quiet"

[settings]
experimental = true
jobs = 8
# trusted_config_paths = [
#   '~/work/my-trusted-projects',
# ]

# [shell_alias]
# ll = "ls -la"
